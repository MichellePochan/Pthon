name = (input('ведите имя :'))
password = (input('ведите пароль :'))

while :
    if len(password) < 8:
        print("Короткий!")
        name = (input('ведите имя :'))
        password = (input('ведите пароль :'))
    elif "123" in password:
        print("Простой!")
        name = (input('ведите имя :'))
        password = (input('ведите пароль :'))
    break
password2 = input('повторите пароль :')

while :

    if password != password2:
        print("не это не он")
        password2 = input('повторите пароль :')
    break

myname = input('ваше имя :')
mypassword = input('пароль :')

while :

    if myname!=name:
        print("не верно ведено имя")
        myname = input('ваше имя :')
        mypassword = input('пароль :')
    elif mypassword!=password:
        print('не верно веден пвроль')
        myname = input('ваше имя :')
        mypassword = input('пароль :')

    break
print("добро пожаловать", name)
